#include "types.h"
#include "user.h"

int main(int argc, char *argv[]) 
{
    for 
    (int i = 0; i < 10; i++) 
    {
        int num = randint() % 10;
        printf(1, "%d\n", num);
    }
    exit();
}